{%- comment -%}
COMMIT NOTE / DESCRIPTION:

1️⃣ Grid Layout:
- This section (.grid-custom) displays products in a responsive grid.
- Uses a for-loop over section.blocks to display 6 product blocks by default.
- Each grid-item includes the product image and title.
- Grid is designed to show 3 columns on desktop, 2 on tablet, and 1 on mobile.

2️⃣ Plus Button:
- Each product image contains a '+' button (.plus-btn).
- Button position is controlled by block settings: button_top, button_bottom, button_left, button_right.
- Allows precise control of the button’s margin and placement over each product image.
- Clicking this button triggers the product popup.

3️⃣ Product Popup:
- The '+' button opens a popup displaying the product details.
- Popup includes:
    • Product image
    • Title
    • Price
    • Description
    • Variant selectors (size, color)
    • Functional "Add to Cart" button
- Popup dynamically loads the correct product data from the clicked button using data-product attribute.

This code ensures a fully responsive product grid with interactive + buttons and a functional Shopify product popup.
{%- endcomment -%}


<section class="grid-custom">
  <div class="grid-wrapper">

    {% for block in section.blocks %}
      {% assign product = all_products[block.settings.product] %}

      {% if product != blank %}
        

        <div class="grid-item">

  <div class="image-wrapper">
    <img 
      src="{{ product.featured_image | image_url: width: 600 }}" 
      alt="{{ product.title | escape }}" 
      width="400" 
      height="320" 
      class="grid-image">


<button 
  type="button" 
  class="plus-btn"
  data-product='{
    "title": "{{ product.title | escape }}",
    "price": {{ product.price}},
    "description": "{{ product.description | strip_newlines | escape }}",
    "variants": {{ product.variants | json }},
    "options": {{ product.options | json }},
    "image": "{{ product.featured_image | image_url: width: 600 }}"
  }'
  style="
    {% if block.settings.button_top != 0 %} top: {{ block.settings.button_top }}px; {% endif %}
    {% if block.settings.button_bottom != 0 %} bottom: {{ block.settings.button_bottom }}px; {% endif %}
    left: {{ block.settings.button_left }}px;
    right: {{ block.settings.button_right }}px;
  ">
  +
</button>

  </div>


  <h3 class="grid-title">
    {{ product.title }}
  </h3>

</div>



      {% endif %}

    {% endfor %}

  </div>
</section>



<!-- GLOBAL POPUP -->
<div id="product-popup" class="popup hidden">
  <div class="popup-overlay"></div>

  <div class="popup-card">
    <button id="close-popup" class="popup-close">×</button>

    <div class="popup-inner">

      <!-- Left: Product Image -->
      <div class="popup-left">
        <img 
          id="popup-image"
          src="{{ product.featured_image | image_url: width: 600 }}"
          alt="Product Image"
          width="600"
          height="400"
          class="popup-product-image">
      </div>

      <!-- Right: Product Info -->
      <div class="popup-right">

        <h2 id="popup-title" class="popup-title"></h2>

        <p id="popup-price" class="popup-price"></p>

        <div id="popup-description" class="popup-description"></div>

      </div>
      

    </div>

        <div id="variant-selectors" class="popup-variants"></div>

        <button class="popup-add-to-cart">
          ADD TO CART →
        </button>y

  </div>
</div>



<style>
  .grid-custom {
    padding: 80px 60px;
  }

  .grid-wrapper {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
  }

  .grid-item {
    text-align: center;
  }

  .grid-image {
    width: 100%;
    height: 320px;
    object-fit: cover;
    border-radius: 8px;
    background-color: #f2f2f2;
  }

  .grid-title {
    margin-top: 15px;
    font-size: 18px;
    font-weight: 600;
  }


  /* + Button CSS*/

  .image-wrapper {
  position: relative;
}

.plus-btn {
  position: absolute;
  width: 40px;
  height: 40px;
  border-radius: 50%;
  border: none;
  background: white;
  color: black;
  font-size: 22px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
}




/* Overlay */
.popup {
  position: fixed;
  inset: 0;
  z-index: 9999;
  display: flex;
  align-items: center;
  justify-content: center;
}

.popup.hidden {
  display: none;
}

.popup-overlay {
  position: absolute;
  inset: 0;
  background: rgba(0,0,0,0.5);
}

/* Card */
.popup-card {
  position: relative;
  background: #fff;
  width: 400px;
  max-width: 30%;
  padding: 30px;
  border: 3px solid #000000;
  z-index: 2;
  box-shadow: 0 10px 40px rgba(0,0,0,0.2);

}

.popup-close {
  position: absolute;
  top: 15px;
  right: 20px;
  border: none;
  background: none;
  font-size: 22px;
  cursor: pointer;
}

/* Layout */
.popup-inner {
  display: flex;
  gap: 40px;
}

.popup-left {
  flex: 1;
}

.popup-product-image {
  width: 100%;
  height: auto;
  object-fit: cover;
}

.popup-right {
  flex: 1;
  display: flex;
  flex-direction: column;
}

/* Typography */
.popup-title {
  font-size: 20px;
  font-weight: 600;
  margin-bottom: 10px;
}

.popup-price {
  font-size: 18px;
  font-weight: 500;
  margin-bottom: 15px;
}

.popup-description {
  font-size: 14px;
  color: #666;
  margin-bottom: 20px;
}

/* Variants */
.popup-variants label {
  font-size: 13px;
  display: block;
  margin-bottom: 5px;
  margin-top: 15px;
}

.popup-variants select {
  width: 100%;
  padding: 10px;
  border: 1px solid #ddd;
}

/* Add to cart */
.popup-add-to-cart {
  margin-top: 25px;
  padding: 14px;
  background: #000;
  color: #fff;
  border: none;
  cursor: pointer;
  font-weight: 600;
  letter-spacing: 1px;
  transition: 0.3s;
}

.popup-add-to-cart:hover {
  background: #333;
}

/* Color Segment Control */

.color-toggle {
  display: flex;
  border: 1px solid #000;
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 15px;
}

.color-btn {
  flex: 1;
  padding: 8px 0;
  border: none;
  background: #fff;
  cursor: pointer;
  font-size: 14px;
  transition: all 0.3s ease;
}

.color-btn:not(:last-child) {
  border-right: 1px solid #000;
}

.color-btn.active {
  background: #000;
  color: #fff;
}




  /* Tablet */
  @media (max-width: 1024px) {
    .grid-wrapper {
      grid-template-columns: repeat(2, 1fr);
    }

      .plus-btn {
    top: 150px !important;   /* change this value */
    left: 150px !important;  /* change this value */
  }
  }

  

  {% comment %} /* Mobile */
  @media (max-width: 768px) {
    .grid-wrapper {
      grid-template-columns: 1fr;
    }
  } {% endcomment %}
</style>


<script>
document.addEventListener("DOMContentLoaded", function() {

  const plusButtons = document.querySelectorAll(".plus-btn");
  const popup = document.getElementById("product-popup");
  const closeBtn = document.getElementById("close-popup");

  const popupImage = document.getElementById("popup-image");
  const popupTitle = document.getElementById("popup-title");
  const popupPrice = document.getElementById("popup-price");
  const popupDescription = document.getElementById("popup-description");
  const variantContainer = document.getElementById("variant-selectors");
  const addToCartBtn = document.querySelector(".popup-add-to-cart");

  let currentProductData = null;

  plusButtons.forEach(button => {
    button.addEventListener("click", function() {
      const productData = JSON.parse(this.dataset.product);
      currentProductData = productData;

      // Set popup content
      popupImage.src = productData.image;
      popupImage.alt = productData.title;
      popupTitle.textContent = productData.title;
      {% comment %} popupPrice.textContent = (productData.price / 100).toFixed(2); {% endcomment %}
       popupPrice.innerText =  "$" + (productData.price/100).toFixed(2);
      popupDescription.innerHTML = productData.description;

      // Clear old variants
      variantContainer.innerHTML = "";

      // Build variants
      if (productData.options.length > 0 && productData.variants.length > 1) {
        const sortedOptions = [...productData.options].sort((a, b) => {
          if (a.toLowerCase() === "color") return -1;
          if (b.toLowerCase() === "color") return 1;
          return 0;
        });

        sortedOptions.forEach(option => {
          const index = productData.options.indexOf(option);
          const label = document.createElement("label");
          label.textContent = option;
          variantContainer.appendChild(label);

          const usedValues = [];
          productData.variants.forEach(variant => {
            const value = variant.options[index];
            if (!usedValues.includes(value)) usedValues.push(value);
          });

          if (option.toLowerCase() === "color") {
            const wrapper = document.createElement("div");
            wrapper.classList.add("color-toggle");

            usedValues.forEach((val, i) => {
              const btn = document.createElement("button");
              btn.type = "button";
              btn.textContent = val;
              btn.classList.add("color-btn");
              if (i === 0) btn.classList.add("active");
              btn.addEventListener("click", () => {
                wrapper.querySelectorAll(".color-btn").forEach(b => b.classList.remove("active"));
                btn.classList.add("active");
              });
              wrapper.appendChild(btn);
            });

            variantContainer.appendChild(wrapper);
          } else {
            const select = document.createElement("select");
            usedValues.forEach(val => {
              const opt = document.createElement("option");
              opt.value = val;
              opt.textContent = val;
              select.appendChild(opt);
            });
            variantContainer.appendChild(select);
          }
        });
      }

      // Show popup
      popup.classList.remove("hidden");
    });
  });

  // Close popup
  closeBtn.addEventListener("click", () => popup.classList.add("hidden"));
  popup.addEventListener("click", e => {
    if (e.target === popup) popup.classList.add("hidden");
  });

  // Add to cart
  addToCartBtn.addEventListener("click", () => {
    if (!currentProductData) return;

    let selectedOptions = [];
    variantContainer.querySelectorAll("select").forEach(sel => selectedOptions.push(sel.value));
    variantContainer.querySelectorAll(".color-toggle").forEach(wrapper => {
      const active = wrapper.querySelector(".color-btn.active");
      selectedOptions.push(active ? active.textContent : null);
    });

    const selectedVariant = currentProductData.variants.find(variant =>
      variant.options.every((opt, i) => opt === selectedOptions[i])
    );

    if (!selectedVariant) {
      alert("Please select a valid variant.");
      return;
    }

    // Add main product
    fetch("/cart/add.js", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ items: [{ id: selectedVariant.id, quantity: 1 }] })
    })
    .then(() => {
      // Check special rule
      const colorIndex = currentProductData.options.findIndex(o => o.toLowerCase() === "color");
      const sizeIndex = currentProductData.options.findIndex(o => o.toLowerCase() === "size");
      const selectedColor = selectedOptions[colorIndex];
      const selectedSize = selectedOptions[sizeIndex];



const bonusVariantId = 43041632747638; // Soft Winter Jacket Black/Medium
// Normalize values
const normalizedColor = selectedColor.trim().toLowerCase();
const normalizedSize = selectedSize.trim().toLowerCase();

// Adjust to match Shopify variant exactly
if (normalizedColor === "black" && normalizedSize === "m") {
  // Add bonus product
  fetch("/cart/add.js", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ items: [{ id: bonusVariantId, quantity: 1 }] })
  })
  .then(() => {
    alert(`${currentProductData.title} AND Soft Winter Jacket added to cart!`);
    popup.classList.add("hidden");
  })
  .catch(err => console.error("Bonus product failed:", err));
} else {
  alert(`${currentProductData.title} added to cart!`);
  popup.classList.add("hidden");
}
    })
    .catch(err => console.error("Main product add failed:", err));
  });

});
</script>




{% schema %}
{
  "name": "Custom Grid",
  "settings": [],
"blocks": [
  {
    "type": "grid_block",
    "name": "Grid Product",
    "settings": [
      {
        "type": "product",
        "id": "product",
        "label": "Select Product"
      },
      {
        "type": "range",
        "id": "button_top",
        "label": "Button Top",
        "min": 0,
        "max": 500,
        "step": 5,
        "unit": "px",
        "default": 0
      },
      {
        "type": "range",
        "id": "button_bottom",
        "label": "Button Bottom",
        "min": 0,
        "max": 500,
        "step": 5,
        "unit": "px",
        "default": 0
      },
      {
        "type": "range",
        "id": "button_left",
        "label": "Button Left",
        "min": 0,
        "max": 500,
        "step": 5,
        "unit": "px",
        "default": 15
      },
      {
        "type": "range",
        "id": "button_right",
        "label": "Button Right",
        "min": 0,
        "max": 500,
        "step": 5,
        "unit": "px",
        "default": 15
      }
    ]
  }
]
,
  "presets": [
    {
      "name": "Custom Grid",
      "blocks": [
        { "type": "grid_block" },
        { "type": "grid_block" },
        { "type": "grid_block" },
        { "type": "grid_block" },
        { "type": "grid_block" },
        { "type": "grid_block" }
      ]
    }
  ]
}
{% endschema %}
